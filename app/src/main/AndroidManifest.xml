<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoFinalGeoterra"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".menu"
            android:exported="true">
        </activity>
        <!-- Actividad de la clase paisMundo -->
        <activity
            android:name=".paisMundo"
            android:exported="true">
        </activity>
        <!-- Actividad de la clase temas -->
        <activity
            android:name=".temas"
            android:exported="true">
        </activity>
        <!-- Actividad de la clase opciones -->
        <activity
            android:name=".opciones"
            android:exported="true">
        </activity>
        <!-- Actividad de la clase registro -->
        <activity
            android:name=".registro"
            android:exported="true">
        </activity>
        <!-- Actividad de la clase registro -->
        <activity
            android:name=".explora"
            android:exported="true">
        </activity>
        <!-- Actividad de la clase registro -->
        <activity
            android:name=".memorama"
            android:exported="true">
        </activity>
    </application>
</manifest>